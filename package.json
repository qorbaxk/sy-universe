{
  "name": "sy-universe",
  "private": true,
  "version": "0.1.0",
  "description": "Obsidian-style portfolio for job switching",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run start:dev -w api",
    "dev:ai": "cd apps/ai && (test -d .venv || python3 -m venv .venv) && .venv/bin/pip install -q -r requirements.txt && .venv/bin/uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "build": "npm run build -w web",
    "build:web": "npm run build -w web",
    "build:api": "npm run build -w api",
    "start:api:prod": "npm run start:prod -w api",
    "preview": "npm run preview -w web",
    "storybook": "npm run storybook -w web"
  },
  "overrides": {
    "@nestjs/common": "11.1.28",
    "@nestjs/core": "11.1.28"
  },
  "engines": {
    "node": ">=20"
  }
}
