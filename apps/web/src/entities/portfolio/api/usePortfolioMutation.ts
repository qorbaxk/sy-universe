/**
 * @alias 포트폴리오 Mutation 훅 자리
 * @description 현재 쓰기 API가 없어 비워 둔다. Contact 제출 등이 생기면
 * `useMutation`을 이 파일에 추가해 재사용한다.
 *
 * @example
 * ```ts
 * export function useSubmitContactMutation() {
 *   return useMutation({ mutationFn: submitContact })
 * }
 * ```
 */
export {}
